# n8n Configuration
N8N_URL=https://n8n.monoliet.cloud
N8N_API_KEY=your-n8n-api-key-here

# MCP Server Configuration
MCP_SERVER_MODE=stdio  # Options: 'stdio' (for Claude Desktop) or 'http' (for remote access)
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8001
LOG_LEVEL=INFO

# Security
MCP_AUTH_TOKEN=generate-secure-token-here

# Django Portal Integration (Phase 4 - Optional for now)
DJANGO_PORTAL_URL=https://portal.monoliet.cloud
DJANGO_WEBHOOK_TOKEN=generate-secure-token-later
DJANGO_INTEGRATION_ENABLED=false

# Optional: Database (if needed later)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname